version: '3.8'

services:
  redis:
    image: redis:7-alpine

  vektor-proxy:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["/app/coordinator"]
    ports:
      - "6380:6380"
      - "9090:9090"
    depends_on:
      - redis
    environment:
      - REDIS_ADDR=redis:6379
